import React, {ReactNode} from 'react';

// Components
import Layout from 'rabbit/common/components/layout';

const Sandbox = (): ReactNode => (
  <Layout>
    <h1 className="mb4 mt4 fz-45 flex">Sandbox</h1>
  </Layout>
);

export default Sandbox;
